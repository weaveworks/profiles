
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: profilesubscriptions.weave.works
spec:
  group: weave.works
  names:
    kind: ProfileSubscription
    listKind: ProfileSubscriptionList
    plural: profilesubscriptions
    singular: profilesubscription
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProfileSubscription is the Schema for the profilesubscriptions
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ProfileSubscriptionSpec defines the desired state of a ProfileSubscription
            properties:
              branch:
                description: 'Branch is the git repo branch containing the profile
                  definition (default: main)'
                type: string
              profileURL:
                description: ProfileURL is a fully qualified URL to a profile repo
                type: string
            type: object
          status:
            description: ProfileSubscriptionStatus defines the observed state of ProfileSubscription
            properties:
              message:
                description: Message is the reason for the "failing" state
                type: string
              state:
                description: State is the current state of the ProfileSubscription
                  Can be "running" or "failing"
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
